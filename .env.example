# No server-side key is required.
# Users enter their own fal API key via the Sign in button; it is
# stored only in the browser's localStorage and attached per request
# to fal.run via the @fal-ai/client. The server never keeps a key.
